# Google Cloud Platform Configuration for Vertex AI
# To get these credentials, run: gcloud auth application-default login
# Make sure the Vertex AI API is enabled in your GCP project.

GOOGLE_CLOUD_PROJECT=your-actual-gcp-project-id
GOOGLE_CLOUD_LOCATION=us-central1
PORT=8080
